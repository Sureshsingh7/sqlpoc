# Backend configuration for HA+DR deployment
# Note: HA+DR uses the SAME state as HA (DR is added to existing HA)
# The state key is set dynamically by workflow to "sqlserver-dev-ha.tfstate"

resource_group_name  = "rg-fnz-poc-tfstate-se"
storage_account_name = "stfnzpocdj522c"
container_name       = "tfstate"
